**Enums.swift**

This Swift code defines a set of constants and credentials using enums.  `Constants` stores string keys like "StationType" and "TermsAndConditionsAccepted", likely for user defaults or other settings.  `Credentials` holds "APP ID" and "APP TOKEN" keys, presumably for authentication.  The `ConstantMessagesParameters` class defines static string variables `CAM_SUBSCRIBE_GROUP` and `CAM_PUBLISH_GROUP`, which appear to be related to message groups, possibly for a communication system.  Using enums and a separate class helps organize and manage these values, making the code more readable and maintainable.


**Extensions.swift**

This Swift code defines several extensions to enhance existing functionalities.  The `Color` extension adds an initializer for creating colors from hexadecimal values and a helper function for safe area compatibility.  The `View` extension provides a landscape preview helper.  The `UIImage` extension adds a `rotate` function to rotate images by a specified radian value.  Finally, the `StationKind` extension adds a `getTextValue` function providing human-readable strings for different station types.  These extensions improve code readability and provide convenient utilities for common tasks.


Generated by Salon from Braid Technologies, 26/02/2025