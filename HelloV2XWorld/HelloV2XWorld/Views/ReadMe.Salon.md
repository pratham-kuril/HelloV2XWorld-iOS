**HomeView.swift**

This SwiftUI code defines a `HomeView` for a V2X (Vehicle-to-Everything) application.  It displays a map view showing CAM (Cooperative Awareness Messages) users.  The view includes zoom in/out buttons and a settings button.  Upon appearance, the `HomeView` starts the CAM service and displays the connection status.  A `ZoomSettings` object manages the map's zoom level.  Annotations show CAM users on the map.  The `sdkInstance` handles the V2X communication logic.  The code also includes preview functionality and reusable button views for the map controls.


**SettingsView.swift**

This Swift code defines a `SettingsView` for a V2X (Vehicle-to-Everything) application.  It allows users to change their station type (e.g., passenger car, bus, pedestrian) via an action sheet. Application ID and Token are displayed but are disabled.  The view uses a ZStack for layering, with a red background and a VStack to arrange the UI elements.  The "Apply" button saves the selected station type to UserDefaults and triggers a reconnect to the MQTT service.  A "Cancel" button dismisses the view.  The `stationTypeButtons` function dynamically generates buttons for the action sheet based on available `StationKind` enum values. A preview is provided for landscape orientation.


**SplashScreenView.swift**

This SwiftUI code defines a splash screen (SplashScreenView) that displays Terms and Conditions from a local PDF file.  Upon first launch, the user must accept the terms.  This acceptance is stored in UserDefaults.  Subsequent launches skip the terms and directly navigate to the HomeView.  The splash screen also initializes a connection to a STEP environment using SDKConfigurator. The view loads a PDF document named "TermsAndConditions.pdf" and displays it using a PDFKitView.  An "Exit" button allows the user to quit the app, and an "Accept" button, enabled after the PDF is loaded, navigates to the HomeView. The app prevents the device from sleeping while the splash screen is displayed.


Generated by Salon from Braid Technologies, 26/02/2025